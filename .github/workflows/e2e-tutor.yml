name: E2E Tutor IA

on:
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  test:
    timeout-minutes: 20
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: 22
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci

    - name: Install Playwright Browsers
      run: npx playwright install --with-deps chromium

    - name: Require authenticated E2E configuration
      shell: bash
      run: |
        missing=0
        for name in E2E_USER_EMAIL E2E_USER_PASSWORD VITE_SUPABASE_URL VITE_SUPABASE_ANON_KEY; do
          if [ -z "${!name}" ]; then
            echo "::error::Missing required repository secret: $name"
            missing=1
          fi
        done
        exit "$missing"
      env:
        E2E_USER_EMAIL: ${{ secrets.E2E_USER_EMAIL }}
        E2E_USER_PASSWORD: ${{ secrets.E2E_USER_PASSWORD }}
        VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
        VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}

    - name: Build project
      run: npm run build
      env:
        VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
        VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}

    - name: Run Playwright tests
      run: |
        npm run preview -- --host 127.0.0.1 --port 4173 &
        npx wait-on http://localhost:4173 --timeout 60000
        npm run test:e2e:tutor > test-output.log 2>&1 || (cat test-output.log && exit 1)
      env:
        E2E_USER_EMAIL: ${{ secrets.E2E_USER_EMAIL }}
        E2E_USER_PASSWORD: ${{ secrets.E2E_USER_PASSWORD }}
        VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
        VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
        PLAYWRIGHT_TEST_BASE_URL: http://localhost:4173
        CI: true

    - name: Save logs and results
      if: always()
      run: |
        mkdir -p artifacts
        if [ -f test-output.log ]; then
          cp test-output.log artifacts/test-output.log
        fi

    - uses: actions/upload-artifact@v4
      if: always()
      with:
        name: playwright-report-tutor
        path: |
          playwright-report/
          artifacts/
        retention-days: 30
